\section{Relações de ordem}\label{sec:relacoesDeOrdem}
\index{ordem}
\index{ordem!boa}
\index{isomorfismo de ordens}
Nesta seção, introduziremos as noções básicas de pré-ordem, ordem parcial e ordem total, bem como suas versões estritas e conceitos associados.

\begin{definition}[Pré-ordem]\label{def:preOrdem}\index{ordem!pré}
    Uma \emph{pré-ordem} é um par $(P, \leq)$ onde $P$ é um conjunto e $\leq$ é uma relação binária em $P$ tal que para todo $x, y, z \in P$:
    \begin{itemize}
        \item $x \leq x$ (reflexividade)
        \item Se $x \leq y$ e $y \leq z$, então $x \leq z$ (transitividade).
    \end{itemize}
\end{definition}

\begin{definition}[Ordem parcial]\label{def:ordemParcial}\index{ordem!parcial}
    Uma \emph{ordem parcial} é um par $(P, \leq)$ onde $P$ é um conjunto e $\leq$ é uma pré-ordem e, além disso, satisfaz a seguinte condição.
    \begin{itemize}
        \item Se $x \leq y$ e $y \leq x$, então $x = y$ (antissimetria).
    \end{itemize}
\end{definition}

Nem toda pré-ordem é uma ordem parcial.
\begin{example}\label{exa:preOrdemUniversal}
    Seja $A$ um conjunto.
    Então $(A, A\times A)$ é uma pré-ordem.
    Se $A$ tem ao menos dois elementos distintos, então $(A, A\times A)$ não é uma ordem parcial.
\end{example}

\begin{definition}[Ordem total]\label{def:ordemTotal}\index{ordem!total}\index{ordem!linear}
    Uma \emph{ordem total}, ou \emph{ordem linear}, é um par $(P, \leq)$ onde $P$ é um conjunto e $\leq$ é uma ordem parcial e, além disso, satisfaz a seguinte condição.
    \begin{itemize}
        \item Para todo $x, y \in P$, ou $x \leq y$ ou $y \leq x$ (totalidade).
    \end{itemize}
\end{definition}

Há também versões estritas.

\begin{definition}[Ordem parcial estrita]\label{def:ordemParcialEstrita}\index{ordem!parcial estrita}
    Uma \emph{ordem parcial estrita} é um par $(P, <)$ onde $P$ é um conjunto e $<$ é uma relação binária em $P$ tal que para todo $x, y, z \in P$, valem as seguintes condições.
    \begin{itemize}
        \item Não existe $x$ tal que $x < x$ (irreflexividade).
        \item Se $x < y$ e $y < z$, então $x < z$ (transitividade).
    \end{itemize}
\end{definition}

\begin{definition}[Ordem total estrita]\label{def:ordemTotalEstrita}\index{ordem!total estrita}
    Uma \emph{ordem total estrita} é um par $(P, <)$ onde $P$ é um conjunto e $<$ é uma ordem parcial estrita e, além disso, satisfaz a seguinte condição.
    \begin{itemize}
        \item Para todo $x, y \in P$, ou $x < y$ ou $y < x$ ou $x = y$ (totalidade).
    \end{itemize}
\end{definition}

As versões estritas e não estritas estão em correspondência natural.

\begin{definition}[Ordem parcial estrita induzida]\label{def:ordemParcialEstritaInduzida}
    Seja $(P, \leq)$ uma ordem parcial.
    A ordem parcial estrita associada a $(P, \leq)$ é o par $(P, <)$ em que $<$ é a relação binária em $P$ definida por $x < y$ se e somente se $x \leq y$ e $x \neq y$.
\end{definition}

\begin{definition}[Ordem parcial não estrita induzida]\label{def:ordemParcialInduzida}
    Seja $(P, <)$ uma ordem parcial estrita.
    A ordem parcial associada a $(P, <)$ é o par $(P, \leq)$ em que $\leq$ é a relação binária em $P$ definida por $x \leq y$ se e somente se $x < y$ ou $x = y$.
\end{definition}

As seguintes duas proposições são deixadas como exercício ao leitor (Exercício~\ref{exr:ordensEstritasENaoEstritas}).

\begin{proposition}\label{prop:ordensEstritasInduzidas}
    Seja $(P, \leq)$ uma ordem parcial.
    \begin{enumerate}[label=(\alph*)]
        \item A ordem parcial estrita induzida por $(P, \leq)$ é de fato uma ordem parcial estrita.
        \item Seja $(P, <)$ a ordem parcial estrita induzida por $(P, \leq)$.
        Então a ordem parcial não estrita induzida por $(P, <)$ é $(P, \leq)$.
        \item $(P, \leq)$ é uma ordem total se e somente se $(P, <)$ é uma ordem total estrita.
    \end{enumerate}
\end{proposition}

Dualmente, temos a seguinte proposição.

\begin{proposition}\label{prop:ordensNaoEstritasInduzidas}
    Seja $(P, <)$ uma ordem parcial estrita.
    \begin{enumerate}[label=(\alph*)]
        \item A ordem parcial induzida por $(P, <)$ é de fato uma ordem parcial.
        \item Seja $(P, \leq)$ a ordem parcial induzida por $(P, <)$.
        Então a ordem parcial estrita induzida por $(P, \leq)$ é $(P, <)$.
        \item $(P, <)$ é uma ordem total estrita se e somente se $(P, \leq)$ é uma ordem total.
    \end{enumerate}
\end{proposition}

Por isso, ao se estudar ordens parciais, não há perda em se restringir ao estudo de ordens estritas ou não estritas.
Além disso, ao longo deste livro, ao estabelecer que $\leq$ é uma ordem parcial, iremos, sem aviso, utilizar a ordem estrita $<$ associada a $\leq$ e vice-versa.
Outros símbolos que poderão ser usados para denotar ordens parciais fazendo jus a esta convenção são $\preceq$ e $\prec$, $\sqsubseteq$ e $\sqsubset$, dentre outros.

\begin{example}\label{exa:ordemParcialDasPartes}
    Seja $A$ qualquer conjunto.
    Define-se $\subseteq_A=\{(X, Y) \in \mathcal{P}(A) \times \mathcal{P}(A) : X \subseteq Y\}$.
    Então $(\mathcal{P}(A), \subseteq_A)$ é uma ordem parcial.
    
    É usual denotá-la por $(\mathcal{P}(A), \subseteq)$, omitindo o subíndice $A$.
\end{example}


Abaixo, definiremos os conceitos de cota superior, cota inferior, máximo, mínimo, elemento maximal, elemento minimal, supremo e ínfimo de um conjunto em uma pré-ordem.
Para ordens parciais estritas, tais conceitos são definidos a partir da ordem parcial não estrita associada.

\begin{definition}\label{def:cotasMaximosSupremos}\index{cota superior}\index{cota inferior}\index{máximo}\index{mínimo}\index{elemento maximal}\index{elemento minimal}\index{supremo}\index{ínfimo}
    Seja $(P, \leq)$ uma pré-ordem e $A \subseteq P$. Dizemos que:
    \begin{enumerate}[label=(\roman*)]
        \item $x$ é uma \emph{cota superior} de $A$ se para todo $a \in A$, $a \leq x$;
        \item $x$ é uma \emph{cota inferior} de $A$ se para todo $a \in A$, $x \leq a$;
        \item $x$ é um \emph{máximo} de $A$, ou um \emph{maior elemento} de $A$, se é uma cota superior de $A$ e $x \in A$;
        \item $x$ é um \emph{mínimo} de $A$, ou um \emph{menor elemento} de $A$, se é uma cota inferior de $A$ e $x \in A$;
        \item $x$ é \emph{maximal} em $A$ se $x \in A$ e para todo $a \in A$, tal que $x \leq a$, segue que $a\leq x$;
        \item $x$ é \emph{minimal} em $A$ se $x \in A$ e para todo $a \in A$, tal que $a \leq x$, segue que $x \leq a$;
        \item $x$ é um \emph{supremo} de $A$ se é uma menor cota superior de $A$, ou seja, $x$ é uma cota superior de $A$ e para toda cota superior $y$ de $A$, segue que $x \leq y$;
        \item $x$ é um \emph{ínfimo} de $A$ se é uma maior cota inferior de $A$, ou seja, $x$ é uma cota inferior de $A$ e para toda cota inferior $y$ de $A$, segue que $y \leq x$;
        \item se $A$ possui uma cota superior, dizemos que $A$ é \emph{limitado superiormente};
        \item se $A$ possui uma cota inferior, dizemos que $A$ é \emph{limitado inferiormente}.
    \end{enumerate}
\end{definition}

Em ordens lineares, não há distinção entre elementos maximais e máximo, ou entre elementos minimais e mínimo.
No entanto, em ordens parciais, pode haver elementos maximais sem que haja um elemento máximo, ou elementos minimais sem que haja um elemento mínimo, como mostra a Figura~\ref{fig:maximaisSemMaximo}.

\begin{center}
\begin{tikzpicture}[
    every node/.style={circle, draw, minimum size=7mm, inner sep=0pt},
    every path/.style={semithick}
]

%--------------------------------
% Esquerda: elementos maximais, mas sem máximo
%--------------------------------
\begin{scope}[xshift=0cm]
    \node (x) at (0,0) {$x$};
    \node (y) at (-1,1.6) {$y$};
    \node (z) at (1,1.6) {$z$};

    \draw (x) -- (y);
    \draw (x) -- (z);
\end{scope}

%--------------------------------
% Direita: existe máximo
%--------------------------------
\begin{scope}[xshift=6cm]
    \node (x2) at (0,0) {$x$};
    \node (y2) at (-1,1.6) {$y$};
    \node (z2) at (1,1.6) {$z$};
    \node (w2) at (0,3.2) {$w$};

    \draw (x2) -- (y2);
    \draw (x2) -- (z2);
    \draw (y2) -- (w2);
    \draw (z2) -- (w2);
\end{scope}

\end{tikzpicture}
\captionsetup{hypcap=false}
\captionof{figure}{À esquerda, há elementos maximais sem elemento máximo. À direita, há um elemento máximo.}
\label{fig:maximaisSemMaximo}
\end{center}

Deixamos também os seguintes fatos básicos para verificação do leitor (Exercício~\ref{exr:propriedadesBasicasDeOrdens}).

\begin{proposition}\label{prop:propriedadesBasicasDeOrdens}
    Seja $(P, \leq)$ uma ordem parcial e $A \subseteq P$.
    \begin{itemize}
        \item Se existe um máximo de $A$, então ele é o único máximo, e o denotamos por $\max A$.
        \item Se existe um mínimo de $A$, então ele é o único mínimo, e o denotamos por $\min A$.
        \item Se existe um supremo de $A$, então ele é o único supremo, e o denotamos por $\sup A$.
        \item Se existe um ínfimo de $A$, então ele é o único ínfimo, e o denotamos por $\inf A$.
        \item Se $x \in A$, então $x$ é um elemento maximal de $A$ se e somente se para todo $a \in A$, se $x \leq a$, então $x=a$.
        \item Se $x \in A$, então $x$ é um elemento minimal de $A$ se e somente se para todo $a \in A$, se $a \leq x$, então $x=a$.
    \end{itemize}
\end{proposition}
Chamaremos de boa ordem uma ordem total estrita em que todo subconjunto não vazio admite um primeiro elemento.
Essa noção desempenhará papel central mais adiante, e será a base para a definição de número ordinal, um dos temas centrais de Teoria dos Conjuntos.
\begin{definition}[Boa ordem]\label{def:boaOrdem}\index{ordem!boa}
    Uma \emph{boa ordem} é uma ordem total estrita $(P, <)$ tal que todo subconjunto não vazio de $P$ possui um elemento mínimo.
\end{definition}
Finalmente, introduzimos a noção de isomorfismo.
\begin{definition}[Isomorfismo entre relações binárias]\label{def:isomorfismoDeRelacoesBinarias}\index{isomorfismo de ordens}\index{homomorfismo de ordens}\index{função!monótona}\index{função!crescente}
    Sejam $P$ e $Q$ conjuntos e $R$ e $S$ relações binárias em $P$ e $Q$, respectivamente.
    Um \emph{isomorfismo} entre $(P, R)$ e $(Q, S)$ é uma bijeção $f : P \to Q$ tal que para todo $x, y \in P$, $x R y$ se e somente se $f(x) S f(y)$.

    Um \emph{homomorfismo} entre $(P, R)$ e $(Q, S)$ é uma função $f : P \to Q$ tal que para todo $x, y \in P$, $x R y$ implica $f(x) S f(y)$.

    Caso $(P, R)$ e $(Q, S)$ sejam pré-ordens, um homomorfismo entre elas é chamado de \emph{função monótona}, ou \emph{função não-decrescente}.

    Caso $(P, R)$ e $(Q, S)$ sejam ordens parciais estritas, um homomorfismo entre elas é chamado de \emph{função estritamente monótona}, ou \emph{função crescente}.
\end{definition}

Para ordens totais, basta a verificação de um dos lados da implicação, como mostra a seguinte proposição.

\begin{proposition}\label{prop:isomorfismoMonotonoEmOrdensTotais}
    Sejam $P$ e $Q$ conjuntos e $\leq$, $\preceq$ ordens totais em $P$ e $Q$, respectivamente.
    Uma bijeção $f : P \to Q$ é um isomorfismo entre $(P, \leq)$ e $(Q, \preceq)$ se e somente se for monótona.
\end{proposition}

\begin{proof}
    A implicação direta é imediata.
    Provaremos a recíproca.
    Basta ver que se $f(x) \preceq f(y)$, então $x \leq y$.
    Assim, fixe $x,y\in P$ tais que $f(x)\preceq f(y)$.

    Suponha, por absurdo, que $x\not\le y$.
    Como $\le$ é uma ordem total, segue que $y<x$.
    Pela hipótese, obtemos $f(y)\preceq f(x)$.
    Como $f$ é injetora e $y\neq x$, temos $f(y)\neq f(x)$.
    Logo, $f(y)\prec f(x)$, em contradição com $f(x)\preceq f(y)$.
    Portanto, $x\le y$.
\end{proof}
\subsection*{Exercícios}
\begin{exercise}\label{exr:ordensEstritasENaoEstritas}
    Prove as proposições \ref{prop:ordensEstritasInduzidas} e \ref{prop:ordensNaoEstritasInduzidas}.
\end{exercise}
\begin{exercise}\label{exr:propriedadesBasicasDeOrdens}
    Prove a Proposição~\ref{prop:propriedadesBasicasDeOrdens}.
\end{exercise}
\begin{exercise}
    Seja $(P, \leq)$ uma ordem total, $A\subseteq P$ e $x \in P$.
    Prove que $x$ é elemento maximal de $A$ se e somente se $x$ é o máximo de $A$.
    Enuncie e prove um resultado análogo para elementos minimais e mínimos.
\end{exercise}

\begin{exercise}
    Seja $(P, \leq)$ uma ordem total.
    Mostre que para todo $x, y \in P$, não vale simultaneamente $x<y$ e $y\leq x$.
\end{exercise}

\begin{exercise}
    Seja $(P, <)$ uma ordem parcial estrita  tal que qualquer conjunto de $2$ elementos de $P$ tem um elemento mínimo.
    Prove que $(P, <)$ é uma ordem total estrita.
\end{exercise}

\begin{exercise}
    Mostre que se $A$ tem dois elementos distintos, então $(\mathcal{P}(A), \subseteq)$ não é uma ordem total.
\end{exercise}

\begin{exercise}
    Seja $A$ um conjunto e considere a ordem parcial $(\mathcal{P}(A), \subseteq)$.
    Prove que para todo $X \subseteq \mathcal{P}(A)$, $\sup X = \bigcup X$ e $\inf X = \bigcap X$.

    Para resolver esse exercício, interprete $\bigcap \emptyset$ como $A$ (intuitivamente, por que isso faz sentido?).
\end{exercise}

\begin{exercise}
    Seja $P$ um conjunto e $Q \subseteq P$.
    Seja $R$ uma relação binária em $P$.
    Prove as seguintes afirmações.

    \begin{itemize}
        \item Se $(P, R)$ é uma pré-ordem, então $(Q, R \cap (Q \times Q))$ é uma pré-ordem.
        \item Se $(P, R)$ é uma ordem parcial, então $(Q, R \cap (Q \times Q))$ é uma ordem parcial.
        \item Se $(P, R)$ é uma ordem total, então $(Q, R \cap (Q \times Q))$ é uma ordem total.
        \item Se $(P, R)$ é uma ordem parcial estrita, então $(Q, R \cap (Q \times Q))$ é uma ordem parcial estrita.
        \item Se $(P, R)$ é uma ordem total estrita, então $(Q, R \cap (Q \times Q))$ é uma ordem total estrita.
    \end{itemize}
\end{exercise}

\begin{exercise}
    Sejam $(P, \leq)$ e $(Q, \preceq)$ ordens parciais e $(P, <)$ e $(Q, \prec)$ as ordens estritas associadas a elas, respectivamente.
    Prove que $f : P \to Q$ é um isomorfismo entre $(P, \leq)$ e $(Q, \preceq)$ se e somente se $f$ é um isomorfismo entre $(P, <)$ e $(Q, \prec)$.
\end{exercise}

\begin{exercise}\label{exr:preOrdemQuocienteIsomorfismo}
    Seja $(P, \leq^*)$ uma pré-ordem.
    Defina uma relação binária $\sim$ em $P$ por
    \[
    x \sim y \iff x \leq^* y \land y \leq^* x.
    \]
    \begin{enumerate}[label=(\alph*)]
        \item Mostre que $\sim$ é uma relação de equivalência em $P$.
        \item Seja $Q=P/{\sim}$ e defina uma relação binária $\leq$ em $Q$ por
        \[
        A \leq B \iff \exists p \in A\, \exists q \in B\, (p \leq^* q).
        \]
        Prove que para todos $x, y \in P$, $[x]_{\sim} \leq [y]_{\sim}$ se e somente se $x \leq^* y$.
        \item Prove que $(Q,\leq)$ é uma ordem parcial.
        \item Prove que a projeção canônica $\pi: P \to Q$ dada por $\pi(x)=[x]_{\sim}$ é monótona e sobrejetora.
    \end{enumerate}
\end{exercise}

\begin{exercise}
    Sejam $(P,<)$ e $(Q,\prec)$ ordens totais estritas.
    Suponha que $f:P\to Q$ seja um isomorfismo.
    Prove que $(P,<)$ é uma boa ordem se e somente se $(Q,\prec)$ é uma boa ordem.
\end{exercise}
